# catch2_benchmark_example

## Install required packages on Ubuntu

```
apt-get install cmake clang-format cmake-format
```

## Set up
```
cmake -B build
```

## Run benchmark

```
cmake --build build --target benchmark
```

## Format code

```
cmake --build build --target format
```
